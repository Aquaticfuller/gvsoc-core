from gvtest.testsuite import *


def testset_build(testset):
    testset.set_name('router_bandwidth')
    testset.set_components(["interco.router_v2.bandwidth"])

    t = testset.new_make_test('done_passthrough', flags='CASE=done_passthrough', build_resource='gvsoc.core.build', no_clean=True)
    t.add_description(
        "Smoke test for the rate-unconstrained pass-through path: with "
        "bandwidth=0 and latency=0 a single 4-byte read completes inline at "
        "the same cycle as its send."
    )

    t = testset.new_make_test('no_deny_burst', flags='CASE=no_deny_burst', build_resource='gvsoc.core.build', no_clean=True)
    t.add_description(
        "Three 16-byte reads issued back-to-back at cycles 10, 11, 12 with "
        "bandwidth=4. Verifies that every request is accepted immediately "
        "(no DENY) but the per-input/per-output watermarks grow each "
        "response's reported latency by ceil(size/bandwidth) = 4 cycles — "
        "i.e. the router pipelines without handshaking."
    )

    t = testset.new_make_test('bandwidth_latency_stack', flags='CASE=bandwidth_latency_stack', build_resource='gvsoc.core.build', no_clean=True)
    t.add_description(
        "Four 16-byte reads issued at cycle 0 to the same target with "
        "bandwidth=4. Expects the responses' reported latencies to stack "
        "as 4, 8, 12, 16, proving the per-output watermark advances by "
        "exactly burst_duration per accepted forward."
    )

    t = testset.new_make_test('latency_annotation', flags='CASE=latency_annotation', build_resource='gvsoc.core.build', no_clean=True)
    t.add_description(
        "One 16-byte read with router latency=2 and bandwidth=4. Expects "
        "reported latency = 2 (router) + 4 (burst_duration) = 6 — verifies "
        "that per-router latency adds on top of the bandwidth contribution "
        "without advancing wall-clock beyond the issue cycle."
    )

    t = testset.new_make_test('input_throttle_two_outputs',
                              flags='CASE=input_throttle_two_outputs',
                              build_resource='gvsoc.core.build',
                              no_clean=True)
    t.add_description(
        "One master pushes three 16-byte reads at cycles 10, 11, 12 to "
        "*three different* outputs. The per-input watermark makes the "
        "master itself the bottleneck, so latencies stack as 4, 7, 10 — "
        "verifies that a single initiator cannot exceed its share of the "
        "global bandwidth even when outputs are free."
    )

    t = testset.new_make_test('series_two_routers', flags='CASE=series_two_routers', build_resource='gvsoc.core.build', no_clean=True)
    t.add_description(
        "Two bandwidth routers in series (each bandwidth=8, latency=1) with a "
        "single 64-byte read. Guards the latency/duration split: head latency "
        "adds across hops (1+1) while bandwidth occupancy max-combines "
        "(max(8,8)), so get_full_latency() = 10 instead of the old "
        "store-and-forward sum 2*(1+8) = 18."
    )

    t = testset.new_make_test('rw_channels_split', flags='CASE=rw_channels_split',
                              checker=_check_rw_channels_split,
                              build_resource='gvsoc.core.build', no_clean=True)
    t.add_description(
        "A 16-byte read then a 16-byte write to the same output with "
        "bandwidth=4 and shared_rw_channel=False (the default). Both must "
        "report latency=4: with independent channels neither direction waits "
        "for the other. Regression guard — the bandwidth variant used to keep "
        "a single watermark per port and ignore shared_rw_channel entirely, "
        "always serialising the write behind the read."
    )

    t = testset.new_make_test('rw_channels_shared', flags='CASE=rw_channels_shared',
                              checker=_check_rw_channels_shared,
                              build_resource='gvsoc.core.build', no_clean=True)
    t.add_description(
        "Same workload with shared_rw_channel=True. Both requests compete for "
        "one watermark, so the write stacks behind the read: 4 and 7."
    )

    t = testset.new_make_test('out_of_mapping', flags='CASE=out_of_mapping', build_resource='gvsoc.core.build', no_clean=True)
    t.add_description(
        "Read at 0x20000000, outside any mapping. Router responds "
        "IO_RESP_INVALID and bumps the ``errors`` stat without forwarding."
    )


def _done_latencies(output: str) -> dict[str, int]:
    """Extract the reported latency of each named request from a stub_master log."""
    import re
    latencies: dict[str, int] = {}
    for line in output.splitlines():
        m = re.search(r'\bDONE name=(\S+) status=0 latency=(\d+)', line)
        if m:
            latencies[m.group(1)] = int(m.group(2))
    return latencies


def _check_rw_channels_split(test, output, *args, **kwargs):
    lat = _done_latencies(output)
    if 'rd' not in lat or 'wr' not in lat:
        return False, f'Missing DONE for rd or wr (got {lat})'
    if lat['rd'] != 4 or lat['wr'] != 4:
        return False, (f'Expected both directions to report latency=4 on separate '
                       f'channels; got rd={lat["rd"]} wr={lat["wr"]}')
    return True, 'rd and wr both completed with latency=4'


def _check_rw_channels_shared(test, output, *args, **kwargs):
    lat = _done_latencies(output)
    if 'rd' not in lat or 'wr' not in lat:
        return False, f'Missing DONE for rd or wr (got {lat})'
    if lat['rd'] != 4 or lat['wr'] != 7:
        return False, (f'Expected rd=4 wr=7 on a shared channel; '
                       f'got rd={lat["rd"]} wr={lat["wr"]}')
    return True, f'rd and wr serialised: rd={lat["rd"]} wr={lat["wr"]}'
